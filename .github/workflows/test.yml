name: Run Tests

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - main

jobs:
  test:
    defaults:
      run:
        shell: bash -el {0}

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
      # Checkout the repository
      - name: Check out repository
        uses: actions/checkout@v3

      # Set up Conda
      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v2
        with:
          python-version: 3.12
          auto-update-conda: true
          environment-file: env.yml
          activate-environment: cellphenoX

      # Print Conda environment info (for debugging)
      - name: Print Conda Environment Info
        run: conda info --envs

      # Install pytest if not in environment.yml
      - name: Install pytest
        run: |
          conda activate cellphenoX
          conda install pytest -y

      # Run tests 
      - name: Run tests
        run: |
          conda activate cellphenoX
          export PYTHONPATH=$(pwd)
          pytest test/
